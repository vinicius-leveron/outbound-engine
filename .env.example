# ============================================================
# MOTOR DE OUTBOUND — Variáveis de Ambiente
# ============================================================
# Copie este arquivo para .env e preencha os valores
# cp .env.example .env
# ============================================================

# --- CRM (Supabase Edge Function) ---
CRM_BASE_URL=https://peegicizxybjgvuutegc.supabase.co/functions/v1/crm-api
CRM_API_KEY=<SUBSTITUIR>

# --- Apify (Scraping) ---
APIFY_TOKEN=<SUBSTITUIR>

# --- Gemini (Análise Multimodal — usado pelo T15) ---
GEMINI_API_KEY=<SUBSTITUIR>

# --- Resend (Email Sending) ---
RESEND_API_KEY=<SUBSTITUIR>

# --- Google Sheets ---
GOOGLE_SHEETS_ID=<SUBSTITUIR>
GOOGLE_OAUTH_CLIENT_ID=<SUBSTITUIR>
GOOGLE_OAUTH_CLIENT_SECRET=<SUBSTITUIR>
GOOGLE_OAUTH_REFRESH_TOKEN=<SUBSTITUIR>

# --- Hunter.io (Email Enrichment) ---
HUNTER_API_KEY=<SUBSTITUIR>

# --- Apollo (Email/Phone Enrichment) ---
APOLLO_API_KEY=<SUBSTITUIR>

# --- Z-API (WhatsApp — opcional) ---
ZAPI_INSTANCE_ID=<SUBSTITUIR>
ZAPI_TOKEN=<SUBSTITUIR>
ZAPI_SECURITY_TOKEN=<SUBSTITUIR>
